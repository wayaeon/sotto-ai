psutil>=5.9
huggingface_hub>=0.23
requests>=2.32
pyaudio>=0.2.14
webrtcvad-wheels>=2.0.14
sherpa-onnx>=1.13,<2
onnx-asr[hub]==0.11.0
onnxruntime>=1.18
numpy>=1.22

# Legacy model adapters are retained for non-Windows builds.  Windows uses
# Parakeet's ONNX runtime only, so these frameworks must not enter its wheel.
faster-whisper>=1.1; sys_platform != "win32"
torch>=2.3; sys_platform != "win32"
torchaudio>=2.3; sys_platform != "win32"
nemo_toolkit[asr]; sys_platform != "win32"
transformers>=5.2,<6; sys_platform != "win32"
accelerate; sys_platform != "win32"
funasr; sys_platform != "win32"
mistral-common[audio]>=1.11.3; sys_platform != "win32"
