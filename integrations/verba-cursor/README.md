# Verba Local Context for Cursor

This companion reports only the active relative filename when Cursor receives focus. It does not read or transmit file contents, selections, workspace file lists, screenshots, or audio.

Open this folder in Cursor and press **F5** to run it in an Extension Development Host. It is plain JavaScript, so no install or compile step is required.
