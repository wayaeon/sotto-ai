# Plan: Features, vocabulary, and durable local data

1. Add failing contracts for the durable store, feature rail, teach toolbar, and Windows device selection.
2. Add a small Tauri JSON store under the user's Documents/Verba folder and TypeScript wrappers with localStorage migration/cache behavior.
3. Add correction rules and vocabulary metadata; apply rules to existing transcripts and future sidecar output.
4. Add the Features hub and rail entry, keeping planned capabilities clearly marked as planned.
5. Verify focused tests, the full Python suite, and the production UI/Rust build.
